<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Action for Book Scraper -->
        <record id="action_book_scrapper" model="ir.actions.act_window">
            <field name="name">Book Scraper</field>
            <field name="res_model">book.scrapper</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_tree_book_scrapper"/>
            <field name="view_ids" eval="[(0, 0, ref('view_tree_book_scrapper')), (0, 0, ref('view_form_book_scrapper'))]"/>
            <field name="type">"ir.actions.act_window"</field>
                <p class="oe_view_nocontent_create">
                    Click to start scraping books!
                </p>
            </field>
        </record>

         Tree View for Book Scraper -->
        <record id="view_tree_book_scrapper" model="ir.ui.view">
            <field name="name">book.scrapper.tree</field>
            <field name="model">book.scrapper</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="books" widget="many2many_tags" options="{'no_create_edit': True}"/>
                </tree>
            </field>
        </record>

        <!-- Form View for Book Scraper -->
        <record id="view_form_book_scrapper" model="ir.ui.view">
            <field name="name">book.scrapper.form</field>
            <field name="model">book.scrapper</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="name"/>
                        <field name="books" widget="many2many_tags" options="{'no_create_edit': True}"/>
                    </group>
                </form>
            </field>
        </record>
        
    </data>
</odoo> -->
